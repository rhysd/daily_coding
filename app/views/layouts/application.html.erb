<!DOCTYPE html>
<html lang="jp">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "DailyCoding" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <% top_path = request.fullpath == root_path ? root_path : problems_today_path %>
          <a class="brand" href=<%= top_path %>><font color="#FFA945">DailyCoding</font></a>
          <ul class="nav secondary-nav" style="float: right;">
            <% if logged_in? %>
              <a href="/profiles/<%= current_user.id %>"><img src="<%= current_user.profile_image_url %>" height="28" vspace="4"></a><%= link_to "Logout", smart_twitter.logout_path, :class => "brand" %>
            <% else %>
              <%= link_to "Login", smart_twitter.login_path, :class => "brand" %>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <%= yield %>
      </div><!--/content-->

      <hr>
      <footer>
      <p>&copy; DailyCoding 2012 <ul style="float: right;"><%= link_to "Project Euler","http://projecteuler.net/" %></ul></p>
      <p>This site is licensed under a <%= link_to "Creative Commons License","http://creativecommons.org/licenses/by-nc-sa/2.1/jp/" %></p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
